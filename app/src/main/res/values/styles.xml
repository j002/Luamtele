<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="MyCustomTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabTextAppearance">@style/MyCustomTabText</item>
        <item name="tabSelectedTextColor">#FFFFFFFF</item>
    </style>

    <style name="MyCustomTabText" parent="TextAppearance.AppCompat.Button">
        <!--<item name="android:textSize">14sp</item>-->
        <item name="android:textColor">#FFAFAFAF</item>
    </style>

    <style name="myRatingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/ratingstars</item>
        <item name="android:minHeight">22px</item>
        <item name="android:maxHeight">22px</item>
    </style>
    <style name="myRatingBar2" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/starj</item>
        <item name="android:minHeight">32px</item>
        <item name="android:maxHeight">32px</item>
    </style>
    <style name="ratingDialog" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/ratingstart</item>
        <item name="android:minHeight">50px</item>
        <item name="android:maxHeight">50px</item>
    </style>

</resources>
